@inherits LayoutComponentBase

<div class="page">
    <FluentLayout>
        <FluentHeader>
            <FluentEmoji Value="@(new Emojis.PeopleBody.Color.Default.Brain())" /> Memory Tester
        </FluentHeader>

        <FluentStack Orientation="Orientation.Horizontal" Width="100%" Class="content">
            <div class="sidebar">
                <NavMenu />
            </div>
            <main>
                @Body
            </main>
        </FluentStack>

        <FluentFooter>
            Version @AppInfo.Current.VersionString
        </FluentFooter>
    </FluentLayout>
</div>

<FluentToastProvider />
<FluentDialogProvider />
<FluentTooltipProvider />
<FluentMessageBarProvider />
<FluentMenuProvider />
